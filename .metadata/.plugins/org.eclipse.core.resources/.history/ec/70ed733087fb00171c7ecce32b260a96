/**
 * 
 */
package domain.manager;

import domain.model.Car;
import domain.model.Motorcycle;
import domain.model.ParkingCell;
import domain.model.Vehicle;

/**
 * @author juan.salazar
 *
 */
public interface DataValidatorManager {
	
	Boolean validVehicle(Vehicle vehicle);
	
	Boolean validVehicle(Car car);
	
	Boolean validVehicle(Motorcycle motorcycle);
	
	Boolean validParkingCell(ParkingCell cell);

}
